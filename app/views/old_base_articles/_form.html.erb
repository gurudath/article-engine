<%= form_for(@base_article) do |f| %>
  <% if @base_article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@base_article.errors.count, "error") %> prohibited this base_article from being saved:</h2>

      <ul>
      <% @base_article.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
